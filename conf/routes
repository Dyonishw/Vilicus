# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

GET     /                           controllers.CreateController.index

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

GET     /create                     controllers.CreateController.listItems
POST    /create                     controllers.CreateController.createItem

GET     /delete                     controllers.DeleteController.showForm
POST    /delete                     controllers.DeleteController.deleteItem

GET     /restore                    controllers.DeleteController.restore
GET     /flush                      controllers.UpdateController.flushItems

GET     /update                     controllers.UpdateController.listItems
POST    /update                     controllers.UpdateController.updateItem
